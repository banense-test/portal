# Portal deployment pipeline
#
# [PASSIVE AT INCEPTION]
# Deployment automation is intentionally not active during Inception.
# The project skeleton and CI build/test pipeline are the only integration
# concerns at this milestone. Deployment design will be refined in
# Elaboration and activated in Transition once the internal Windows Server
# target and hand-over procedure are confirmed with the Infrastructure team.
#
# See docs/DEPLOYMENT_STRATEGY.md for the evolving deployment plan.
#
name: Portal Deploy

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Deployment placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Explain passive state
        run: |
          echo "Deployment pipeline is passive at Inception."
          echo "Activate in Transition after confirming Windows Server target and hand-over."
