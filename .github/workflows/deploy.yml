# Deployment workflow skeleton for Portal
# Triggered manually or by SCM release creation in Transition.
# This is a placeholder for Elaboration/Construction refinement.

name: Portal Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment (staging | production)'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      release_tag:
        description: 'SCM release tag to deploy'
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    steps:
      - name: Download release artifact
        run: echo "TODO: download artifact for ${{ github.event.inputs.release_tag }}"

      - name: Deploy to internal Windows Server
        run: echo "TODO: deploy to ${{ github.event.inputs.environment }} via Infrastructure team process"

      - name: Smoke test
        run: echo "TODO: run smoke tests against deployed portal"
