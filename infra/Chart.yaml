# Helm chart skeleton — NOT USED for Portal.
# Portal deployment is custom-built to a single internal Windows Server.
# This file is retained as a deliberate negative record so that no role
# introduces container orchestration without a Change Request.

apiVersion: v2
name: portal
# OMITTED: container orchestration is out of scope per CON-007.
# If a future Change Request moves deployment to Kubernetes, this skeleton
# can be activated and versioned.
