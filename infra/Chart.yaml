# Portal deployment packaging metadata
# This file reserves the infrastructure packaging location.
# Detailed deployment packaging (MSDeploy, zip, or container) will be selected in Elaboration/Transition.

apiVersion: v2
name: portal
description: Cuba Corp Employee Portal deployment package
type: application
version: 0.1.0
appVersion: "0.1.0"

# NOTE: This is a skeleton placeholder. The project uses a single-server
# Windows Server deployment (CON-007). Helm/Kubernetes is NOT in scope.
# This file marks the infra/ directory for future deployment artifacts.
